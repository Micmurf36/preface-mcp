# Core runtime
mcp[cli]>=1.2.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# LLM providers (all three are optional — only the one you configure is used)
anthropic>=0.40.0
openai>=1.30.0
httpx>=0.27.0
